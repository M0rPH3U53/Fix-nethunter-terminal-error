# Origine du problème 

Le problème au démarrage de `nhterm` est qu’il ne trouve pas de binaire `SU` a cause de la version 30.6 de Magisk

Il faut donc décompiler le apk d’origine et modifier 2 fichier
